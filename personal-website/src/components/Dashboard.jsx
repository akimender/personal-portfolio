import React from 'react';

const Dashboard = () => {
    const styles = {

    };

    return (
        <div className="dashboard-container">
            <h1>Dashboard</h1>
        </div>
    );
};

export default Dashboard;